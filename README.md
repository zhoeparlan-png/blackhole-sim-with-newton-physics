# blackhole sim with newton physics

Black Hole 2D Physics Simulation

Simulasi interaktif lubang hitam menggunakan Python dan Pygame.
Fitur:

  Physics engine berbasis hukum gravitasi Newtonian.
  Efek visual orbital mechanics (fenomena photon sphere).

Cara Menjalankan:

  Install pygame: pip install pygame
  Jalankan: py -3.xx blackhole2D_v2.py
  
untuk linux:

  buat venv
    `python3 -m venv venv`
    
  aktifkan venv
    `source .venv/bin/activate`
    
  install pygame
    `pip install pygame`
    
  cara menjalankan untuk linux:
    `python3 blackhole_2D_v2.py`

# Arabic language:

محاكاة فيزيائية لثقب أسود (ثنائية الأبعاد)

محاكاة تفاعلية لثقب أسود باستخدام لغة Python ومكتبة Pygame.
المميزات:

  محرك فيزيائي يعتمد على الجاذبية النيوتونية.
  تأثيرات بصرية تحاكي ميكانيكا المدارات (ظاهرة "كرة الفوتونات" أو Photon Sphere).

طريقة التشغيل:

  تثبيت مكتبة pygame: `pip install pygame`
  التشغيل: `py -3.xx blackhole2D_v2.py`

لنظام Linux:

  إنشاء بيئة افتراضية (venv):
    `python3 -m venv venv`

  تفعيل البيئة الافتراضية:
    `source .venv/bin/activate`
  تثبيت مكتبة pygame:
    `pip install pygame`

  طريقة التشغيل على Linux:
    `python3 blackhole_2D_v2.py`

# Japanese language:

ブラックホール2D物理シミュレーション

PythonとPygameを使用した、インタラクティブなブラックホール・シミュレーションです。
主な機能:

  ニュートン力学（万有引力）に基づく物理エンジン。
  軌道力学を表現する視覚効果（光子球現象）。

実行方法:

  pygameのインストール: `pip install pygame`
  実行: `py -3.xx blackhole2D_v2.py`

Linuxの場合:

  仮想環境（venv）の作成
    `python3 -m venv venv`

  仮想環境の有効化
    `source .venv/bin/activate`
  pygameのインストール
    `pip install pygame`

  Linuxでの実行方法:
    `python3 blackhole_2D_v2.py`

# Malaysia language:

Simulasi Fizik 2D Lubang Hitam

Simulasi lubang hitam interaktif menggunakan Python dan Pygame.
ciri-ciri:

  Enjin fizik berdasarkan graviti Newtonian.
  Kesan visual untuk mekanik orbit (fenomena sfera foton).

Cara Berlari:

  Pasang pygame: 
  `pip install pygame`
  Jalankan: `py -3.xx blackhole2D_v2.py`
  
Untuk Linux:

  Buat venv
    `python3 -m venv venv`
    
  Aktifkan venv
    `source .venv/bin/activate`
  Pasang pygame
    `pip install pygame`
    
  Cara berjalan di Linux:
    `python3 blackhole_2D_v2.py`
# English language:

Black Hole 2D Physics Simulation

An interactive black hole simulation using Python and Pygame.
Features:

  Physics engine based on Newtonian gravity.
  Visual effects for orbital mechanics (photon sphere phenomenon).

How to Run:

  Install pygame: `pip install pygame`
  Run: `py -3.xx blackhole2D_v2.py`
  
For Linux:

  Create a venv:
    `python3 -m venv venv`
    
  Activate the venv:
    `source /path/to/your/folder/.venv/bin/activate`
    
  Install pygame:
    `pip install pygame`
    
  How to run on Linux:
    `python3 blackhole_2D_v2.py`
